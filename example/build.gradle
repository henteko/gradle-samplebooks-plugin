buildscript {
  repositories {
    mavenCentral() 
    mavenLocal()
  }
  dependencies { 
    classpath 'com.henteko07:gradle:0.2'
  }
}

apply plugin: 'sample'

sample {
  configKey = "test config key"

  books {
    quickStart {
      title = "quick start book"
      body = "quick start book body"
    }
    userGuide {
      title = "user guide book"
      body = "user guide book body"
    }
  }
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.7'
}
